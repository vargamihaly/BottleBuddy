# Docker Environment Configuration
# Copy this file to .env.docker and fill in your values

# Database Configuration
DB_PASSWORD=mysecretpassword

# JWT Secret (must be at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-for-security

# Google OAuth (optional - leave empty if not using)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production

# API URL for frontend
# Leave empty for production (uses Nginx reverse proxy at /api/*)
# For development, set to http://localhost:3668 to connect directly to API
VITE_API_URL=
